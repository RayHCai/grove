{
    "name": "@platform/project",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "The authoring shape: the project manifest an editor saves, its validator and format migrations, and the adapters that narrow it to what a runtime and a renderer each need.",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc -b",
        "typecheck": "tsc -p tsconfig.test.json",
        "test": "vitest run --passWithNoTests",
        "clean": "rm -rf dist .turbo"
    },
    "dependencies": {
        "@platform/transport": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^7.0.2",
        "vitest": "^4.1.11"
    }
}
