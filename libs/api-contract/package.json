{
    "name": "@grove/api-contract",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "The request and response shapes both services validate against and every caller parses with; the token signer is behind ./tokens, so a browser taking the shapes takes no node:crypto with them.",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        },
        "./tokens": {
            "types": "./dist/session-token.d.ts",
            "import": "./dist/session-token.js"
        }
    },
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc -b",
        "typecheck": "tsc -p tsconfig.test.json",
        "test": "vitest run --passWithNoTests",
        "clean": "rm -rf dist .turbo"
    },
    "dependencies": {
        "zod": "^4.5.4"
    },
    "devDependencies": {
        "@types/node": "^26.1.2",
        "typescript": "^7.0.2",
        "vitest": "^4.1.10"
    }
}
