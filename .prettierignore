node_modules
dist
.turbo
pnpm-lock.yaml
*.log

# Benchmark results, written by a tool and never edited by hand.
bench/runs

# Local agent/editor tooling config, not project source.
.claude

# A symlink to AGENTS.md — formatting it rewrites the link target, not a file.
CLAUDE.md

# Cargo's output, and a lockfile prettier did not write.
target
apps/grove/Cargo.lock

# Terraform's provider plugins and module copies, fetched per root by `terraform init`.
.terraform

# Wire vectors compared byte for byte against `encoding/json`'s output, which has no whitespace.
libs/api-contract/fixtures/wire

# Prisma's generated client: written by a tool, and it carries its own formatting header.
apps/grove/api/src/generated

# SVG is XML; prettier has no parser for it without a plugin this repo does not install.
*.svg
