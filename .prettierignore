node_modules
dist
.turbo
pnpm-lock.yaml
*.log

# Local agent/editor tooling config, not project source.
.claude

# A symlink to AGENTS.md — formatting it rewrites the link target, not a file.
CLAUDE.md
