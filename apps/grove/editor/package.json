{
    "name": "@grove/editor",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "The game editor: signs a creator in, opens their game out of object storage, saves and publishes it, and compiles it here into a local version of the game.",
    "scripts": {
        "dev": "pnpm --filter @grove/ui build && vite",
        "build": "vite build",
        "typecheck": "tsc -p tsconfig.test.json && tsc -p tsconfig.creator.json",
        "test": "vitest run --passWithNoTests",
        "clean": "rm -rf dist .turbo",
        "try": "pnpm --filter @grove/ui build && vite --open /try.html"
    },
    "dependencies": {
        "@grove/api-contract": "workspace:*",
        "@grove/player": "workspace:*",
        "@grove/ui": "workspace:*",
        "@platform/engine": "workspace:*",
        "@platform/project": "workspace:*",
        "@platform/scripting": "workspace:*",
        "monaco-editor": "^0.56.0",
        "react": "^19.2.8",
        "react-dom": "^19.2.8"
    },
    "devDependencies": {
        "@types/react": "^19.2.18",
        "@types/react-dom": "^19.2.4",
        "@vitejs/plugin-react": "^6.0.5",
        "jsdom": "^30.1.0",
        "typescript": "^7.0.2",
        "vite": "^8.2.0",
        "vitest": "^4.1.10"
    }
}
