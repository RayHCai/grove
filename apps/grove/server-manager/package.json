{
    "name": "@grove/server-manager",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "The agent on one fleet host: spawns game processes, caps them, reaps them, reports capacity.",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.test.json",
        "test": "vitest run --passWithNoTests",
        "clean": "rm -rf dist .turbo"
    },
    "devDependencies": {
        "@types/node": "^22.10.5",
        "typescript": "^7.0.2",
        "vitest": "^4.1.10"
    }
}
