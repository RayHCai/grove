{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "typecheck": {
            "inputs": ["**/*.tf", "**/*.tftpl", "**/*.tfvars", "package.json"],
            "outputs": []
        }
    }
}
