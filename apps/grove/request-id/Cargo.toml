[package]
name = "grove-request-id"
description = "The correlation id the fleet's Rust services read, mint and echo, held once for both."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
name = "request_id"
path = "src/lib.rs"

[lints]
workspace = true
