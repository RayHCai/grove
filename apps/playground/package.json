{
    "name": "@platform/playground",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "Multi-pane test harness.",
    "scripts": {
        "dev": "tsc -p tsconfig.server.json && vite",
        "dev:server": "tsc -p tsconfig.server.json && node dist/server/main.js",
        "build": "tsc -p tsconfig.server.json && vite build",
        "preview": "vite preview",
        "typecheck": "tsc -p tsconfig.server.json && tsc -p tsconfig.test.json",
        "test": "tsc -p tsconfig.server.json && vitest run --passWithNoTests",
        "clean": "rm -rf dist .turbo"
    },
    "dependencies": {
        "@platform/client": "workspace:*",
        "@platform/core": "workspace:*",
        "@platform/engine": "workspace:*",
        "@platform/glue": "workspace:*",
        "@platform/math": "workspace:*",
        "@platform/project": "workspace:*",
        "@platform/protocol": "workspace:*",
        "@platform/renderer": "workspace:*",
        "@platform/scripting": "workspace:*",
        "@platform/sim": "workspace:*",
        "@platform/transport": "workspace:*",
        "react": "^19.2.8",
        "react-dom": "^19.2.8"
    },
    "devDependencies": {
        "@types/react": "^19.2.18",
        "@types/react-dom": "^19.2.4",
        "@vitejs/plugin-react": "^6.0.5",
        "typescript": "^7.0.2",
        "vite": "^8.2.0",
        "vitest": "^4.1.10"
    }
}
